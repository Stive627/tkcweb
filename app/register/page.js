import React, { Suspense } from 'react'
import Register from '../../Components/Register/Register'

function Home() {
  return (
    <Suspense>
      <Register/>
    </Suspense>
  )
}

export default Home
